"""EXPERIMENT_TITLE — line marker. Renames the current experiment."""

import sys
import time

print("Renaming this experiment to reflect the actual hypothesis.", flush=True)
time.sleep(0.5)
print("[EXPERIMENT_TITLE] ADX+FastD baseline on BTC/USDT 1h", flush=True)
sys.exit(0)
