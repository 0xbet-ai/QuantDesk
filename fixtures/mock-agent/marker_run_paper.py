"""RUN_PAPER — line marker. Server starts a paper trading session."""

import sys
import time

print("Starting the paper trading session now.", flush=True)
time.sleep(0.5)
print("[RUN_PAPER] AdxFastdBaseline", flush=True)
sys.exit(0)
